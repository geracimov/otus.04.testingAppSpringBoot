spring:
  shell:
    interactive:
      enabled: false
    command:
      help:
        enabled: false
testapp:
  csvPath: classpath:CSVtest
  globTemplate: "*.{csv}"
  separator: ","
  isCorrectSuffix: "\\ *\\* *$"
  inputFromFile: classpath:InputTest/inputFullData.txt